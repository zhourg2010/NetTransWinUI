<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        .nav-item — used for the left rail (Active/Completed/Scheduled/History,
        categories, Settings) and the Settings dialog's left nav. A ToggleButton
        whose IsChecked drives the 3x16 accent "rail" indicator and a persistent
        selected-tint layer that sits under the transient hover/press layer so
        the two states don't fight over the same brush.
    -->
    <Style x:Key="RailNavItemStyle" TargetType="RadioButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="Padding" Value="0,0,12,0"/>
        <Setter Property="CornerRadius" Value="{ThemeResource RadiusControl}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid x:Name="RootGrid" CornerRadius="{TemplateBinding CornerRadius}">
                        <Border x:Name="SelectedLayer"
                                Background="{ThemeResource BgSelectedBrush}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Opacity="0"/>
                        <Border x:Name="HoverLayer"
                                Background="Transparent"
                                CornerRadius="{TemplateBinding CornerRadius}"/>
                        <Grid Padding="{TemplateBinding Padding}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="14"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="Rail" Grid.Column="0"
                                    Width="3" Height="0" CornerRadius="2"
                                    Background="{ThemeResource AccentBrush}"
                                    HorizontalAlignment="Left" Margin="4,0,0,0"/>
                            <ContentPresenter Grid.Column="1"
                                               Content="{TemplateBinding Content}"
                                               VerticalAlignment="Center"/>
                        </Grid>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="HoverLayer.Background" Value="{ThemeResource BgHoverBrush}"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="HoverLayer.Background" Value="{ThemeResource BgPressBrush}"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Target="RootGrid.Opacity" Value="0.4"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="CheckStates">
                                <VisualState x:Name="Unchecked"/>
                                <VisualState x:Name="Checked">
                                    <VisualState.Setters>
                                        <Setter Target="SelectedLayer.Opacity" Value="1"/>
                                        <Setter Target="Rail.Height" Value="16"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Indeterminate"/>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="NavHeadingTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="CharacterSpacing" Value="80"/>
        <Setter Property="Foreground" Value="{ThemeResource Text3Brush}"/>
        <Setter Property="Padding" Value="12,14,12,6"/>
    </Style>

</ResourceDictionary>
